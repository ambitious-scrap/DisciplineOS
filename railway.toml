[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter @disciplineos/shared build && pnpm --filter @disciplineos/server build"

[deploy]
startCommand = "pnpm --filter @disciplineos/server start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
